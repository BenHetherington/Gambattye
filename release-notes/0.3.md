- Add an app icon and file icons (for .gb, .gbc, .sav, .rtc, and .gqs).
- Add save states, which can be accessed using the number keys.
    - Save a state by pressing a number while holding ⌥, and load a state by pressing a number on its own.
    - They can also be accessed by using the Touch Bar, if you have a recent MacBook Pro!
- Autosave save data.
    - This is now done when the window loses focus, in addition to when it's closed.
- Automatically hide the mouse cursor when playing in full-screen.
- Efficiency improvements when Gambattye isn't active or isn't visible.
- Other minor tweaks and bug fixes.
